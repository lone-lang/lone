(import (lone print set lambda if let) (math - <=))

; the iteration count is explained in the README

(set loop (lambda (n)
  (if (<= n 0)
      0
      (let (m (- n 1))
        (loop m)))))

(print (loop 20000))
