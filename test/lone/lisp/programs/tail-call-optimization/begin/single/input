(import (lone print set lambda if begin) (math - <=))

; the iteration count is explained in the README

(set loop (lambda (n)
  (if (<= n 0)
      0
      (begin (loop (- n 1))))))

(print (loop 20000))
