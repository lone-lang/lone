(import (lone print set lambda if generator yield) (math + - <))

(set deep-yield (lambda (n)
  (if (< n 1)
      (yield n)
      (deep-yield (- n 1)))))

(set g (generator deep-yield 50))

(print (g))
