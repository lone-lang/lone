(import (lone print set intercept lambda quote generator yield))

(set g ())

(set g (generator (lambda ()
  (yield 1)
  (g))))

(g)

(print
  (intercept
    (('generator-reentry (lambda (v) 42)))
    (g)))
