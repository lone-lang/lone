(import (lone print intercept lambda quote quasiquote) (vector each))

; handler provides a replacement argument list including a function value
; each iterates over the supplied vector

(intercept
  (('arity-error (lambda (v k) (k (quasiquote ([10 20 30] (unquote print)))))))
  (each))
