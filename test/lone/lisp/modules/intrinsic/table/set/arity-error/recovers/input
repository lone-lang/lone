(import (lone print intercept lambda quote) (table set))

; handler provides a replacement argument list
; set retries and returns the stored value

(print
  (intercept
    (('arity-error (lambda (v k) (k (quote ({} "key" 42))))))
    (set)))
