(import (lone print intercept lambda quote) (table count))

; handler provides a replacement argument list
; count returns the number of entries

(print
  (intercept
    (('arity-error (lambda (v k) (k (quote ({a 1 b 2 c 3}))))))
    (count)))
