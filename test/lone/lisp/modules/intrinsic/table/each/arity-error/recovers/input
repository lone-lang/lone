(import (lone print intercept lambda quote quasiquote) (table each))

; handler provides a replacement argument list including a function value
; each iterates over the supplied table

(intercept
  (('arity-error (lambda (v k) (k (quasiquote ({a 1} (unquote print)))))))
  (each))
