(import (lone print intercept lambda quote) (text code-point-at))

; handler provides text to replace the non-text argument
; code-point-at returns the code point at the supplied index

(print
  (intercept
    (('type-error (lambda (v k) (k "hello"))))
    (code-point-at 0 1)))
