(import (lone print intercept lambda quote) (text code-unit-count))

; handler provides text to replace the non-text argument
; code-unit-count returns the byte size for the replacement

(print
  (intercept
    (('type-error (lambda (v k) (k "hello"))))
    (code-unit-count 0)))
