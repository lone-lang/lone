(import (lone print intercept lambda quote) (text code-point-count))

; handler provides text to replace the non-text argument
; code-point-count returns the count for the replacement

(print
  (intercept
    (('type-error (lambda (v k) (k "hello"))))
    (code-point-count 0)))
