(import (lone print intercept lambda quote) (text slice))

; handler provides an integer to replace the non-integer start
; slice produces the substring starting at the replacement

(print
  (intercept
    (('type-error (lambda (v k) (k 1))))
    (slice "hello" 'x 4)))
