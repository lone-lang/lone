(import (lone print intercept lambda quote) (text slice))

; handler provides a valid index to replace the out-of-range end
; slice produces the requested substring

(print
  (intercept
    (('index-error (lambda (v k) (k 4))))
    (slice "hello" 1 99)))
