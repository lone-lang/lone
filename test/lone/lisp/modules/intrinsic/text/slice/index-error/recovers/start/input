(import (lone print intercept lambda quote) (text slice))

; handler provides a valid index to replace the out-of-range start
; slice produces the requested substring

(print
  (intercept
    (('index-error (lambda (v k) (k 1))))
    (slice "hello" -1 4)))
