(import (lone print intercept lambda quote) (text slice))

; handler provides a replacement argument list
; slice retries and returns the requested substring

(print
  (intercept
    (('arity-error (lambda (v k) (k (quote ("hello" 1 4))))))
    (slice)))
