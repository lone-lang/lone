(import (lone print intercept lambda quote) (math sign))

; handler provides a replacement argument list
; sign returns the sign of the supplied integer

(print
  (intercept
    (('arity-error (lambda (v k) (k (quote (-42))))))
    (sign)))
